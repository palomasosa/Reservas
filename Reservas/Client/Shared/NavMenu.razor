<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">Reservas</a>
        <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
</div>

<div class="@NavMenuCssClass" @onclick="ToggleNavMenu">
    <nav class="flex-column">

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="Crear">
                <span class="oi oi-plus" aria-hidden="true"></span> Nueva Reserva
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="Reservas">
                <span class="oi oi-list-rich" aria-hidden="true"></span> Reservas
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="Huespedes">
                <span class="oi oi-people" aria-hidden="true"></span> Huespedes
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="Responsables">
                <span class="oi oi-person" aria-hidden="true"></span> Responsables 
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="Alojamientos">
                <span class="oi oi-home" aria-hidden="true"></span> Alojamientos
            </NavLink>
        </div>
    </nav>
</div>

@code {
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
